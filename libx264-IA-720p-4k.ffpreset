#
# no b-frames -- keep the profile simple!
# no cabac arithimetic coding. (corresponds to specs for level 1.3 video)
#
coder=0
bf=0
refs=1
level=31
b=4096000
maxrate=4500000
bufsize=8192000
wpredp=0
vprofile=baseline

# mar2012: no longer compatible
# (ie: not needed/desirable, i hope!)
# (params to libx264 (per "Copyleft", etc.) are identical, so likely !needed)
#flags2=-wpred-dct8x8
