diff --git a/libavformat/mpegts.c b/libavformat/mpegts.c
index 85b5146..6bfb040 100644
--- a/libavformat/mpegts.c
+++ b/libavformat/mpegts.c
@@ -2125,6 +2125,7 @@
         return;
     pcr_pid &= 0x1fff;
     add_pid_to_pmt(ts, h->id, pcr_pid);
+    av_log(NULL, AV_LOG_DEBUG, "PAT [programid] [pid] [kind]: %d %d %s\n", h->id, pcr_pid, "PCR");
     update_av_program_info(ts->stream, h->id, pcr_pid, h->version);

     av_log(ts->stream, AV_LOG_TRACE, "pcr_pid=0x%x\n", pcr_pid);
@@ -2256,6 +2257,7 @@
             mpegts_set_stream_info(st, pes, stream_type, prog_reg_desc);

         add_pid_to_pmt(ts, h->id, pid);
+av_log(NULL, AV_LOG_DEBUG, "PAT [programid] [pid] [kind]: %d %d %s\n", h->id, pid, (st->codec->codec_type==1 ? "A" : (st->codec->codec_type==0 ? "V" : "?")));

         av_program_add_stream_index(ts->stream, h->id, st->index);

@@ -2350,6 +2352,7 @@
             add_pat_entry(ts, sid);
             add_pid_to_pmt(ts, sid, 0); // add pat pid to program
             add_pid_to_pmt(ts, sid, pmt_pid);
+av_log(NULL, AV_LOG_DEBUG, "PAT [programid] [pid] [kind]: %d %d %s\n", sid, pmt_pid, "PMT");
         }
     }

@@ -2430,6 +2433,7 @@
                     break;
                 name = getstr8(&p, p_end);
                 if (name) {
+                  av_log(NULL, AV_LOG_DEBUG, "SDT service_name:%s service_provider:%s\n", name, provider_name);
                     AVProgram *program = av_new_program(ts->stream, sid);
                     if (program) {
                         av_dict_set(&program->metadata, "service_name", name, 0);
